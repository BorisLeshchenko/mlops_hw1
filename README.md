# MLOps Домашнее задание №1: Создание воспроизводимого ML-pipeline с использованием DVC и MLflow на примере Iris Classification Pipeline

## Цель проекта

Создание воспроизводимого ML пайплайна с версионированием данных (DVC), логированием экспериментов (MLflow) и полным контролем версий через Git.


## Структура проекта
mlops_hw1/  
├── data/  
│ ├── raw/ # Сырые данные (управляются DVC)  
│ │ ├── iris.csv # Iris dataset (локально)  
│ │ └── iris.csv.dvc # Метаданные DVC  
│ └── processed/ # Обработанные данные (генерируются при dvc repro)  
│ ├── X_train.pkl  
│ ├── X_test.pkl  
│ ├── y_train.pkl  
│ ├── y_test.pkl  
│ └── .gitignore # Игнорирует артефакты DVC  
├── src/  
│ ├── prepare.py # Подготовка данных: split train/test  
│ └── train.py # Обучение модели с логированием MLflow  
├── models/  
│ ├── model.pkl # Обученная модель (генерируется при dvc repro)  
│ └── .gitignore # Игнорирует артефакты DVC  
├── .dvcignore # Конфиг DVC (какие файлы не версионировать)  
├── .gitignore # Глобальные правила игнорирования (Python, MLflow и т.д.)  
├── dvc.yaml # Определение DVC пайплайна (стадии prepare и train)  
├── dvc.lock # Хэши зависимостей (сгенерирован автоматически)  
├── params.yaml # Гиперпараметры пайплайна  
├── requirements.txt # Python зависимости  
└── README.md # Этот файл  

## Как запустить
1. Клонируйте и перейдите в проект
git clone git@github.com:BorisLeshchenko/mlops_hw1.git
cd mlops_hw1

2. Создайте виртуальное окружение и установите зависимости
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

3. Получите данные и запустите пайплайн
dvc pull
dvc repro

4. Откройте MLflow UI
mlflow ui --backend-store-uri sqlite:///mlflow.db

## Краткое описание пайплайна

**Стадия `prepare`:** Загружает `data/raw/iris.csv` и разбивает на train/test (80/20).
Выходы: `data/processed/X_train.pkl`, `X_test.pkl`, `y_train.pkl`, `y_test.pkl`.

**Стадия `train`:** Обучает `LogisticRegression` на обработанных данных.
Логирует метрики (accuracy), параметры, артефакты (модель, матрица ошибок) в MLflow.
Выход: `models/model.pkl` (accuracy: 1.0000).

## 🔍 MLflow Tracking UI

После запуска `dvc repro` откройте MLflow:
mlflow ui --backend-store-uri sqlite:///mlflow.db

Перейдите на `http://localhost:5000` и найдите experiment `iris_classification`.
Там вы увидите:
- **Параметры:** model, random_state, max_iter, split_ratio
- **Метрики:** accuracy
- **Артефакты:** model.pkl, confusion_matrix.png
![alt text](image.png)